using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelectedItem : MonoBehaviour
{
    
    private void OnTriggerEnter(Collider collider)
    {
        if (CompareTag(Tags.ColaMaker))
        {
            
        }
    }
}
